{
  "name": "module-federation-from-scratch",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"lerna run --parallel start",
    "build":"lerna run --parallel build",
    "serve":"lerna run --parallel serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "html-webpack-plugin": "^5.3.2",
    "lerna": "^4.0.0"
  }
}
